<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">    <!-- This creates the "3 dots" or a specific icon for the dropdown -->
    <item
        android:id="@+id/action_theme"
        android:title="Theme"
        android:icon="@drawable/outline_accessible_menu_24"
        app:showAsAction="always">

        <!-- The Dropdown Options -->
        <menu>
            <item
                android:id="@+id/theme_system"
                android:title="System Default" />
            <item
                android:id="@+id/theme_light"
                android:title="Light Mode" />
            <item
                android:id="@+id/theme_dark"
                android:title="Dark Mode" />
        </menu>
    </item>
</menu>
