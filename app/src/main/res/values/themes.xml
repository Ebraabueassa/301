<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CommUnity" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CommUnity" parent="Base.Theme.CommUnity" />


    <!-- popup menu for the accessibility menu -->
    <style name="CustomToolbarTheme" parent="ThemeOverlay.AppCompat.DayNight.ActionBar">
        <item name="android:colorBackground">@color/headerPrimary</item>
        <item name="android:textColor">@color/textPrimary</item>

    </style>

    <!-- popup menu for the accessibility icon -->
    <style name="customPopupTheme" parent="MaterialAlertDialog.Material3">
        <item name="actionBarItemBackground">@color/headerPrimary</item>
        <item name="android:textColorSecondary">@color/button_text_color</item>

    </style>

</resources>