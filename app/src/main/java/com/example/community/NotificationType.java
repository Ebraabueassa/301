package com.example.community;

/**
 * Enum representing types of notifications.
 * Includes win, lose, broadcast, and info notifications.
 */
public enum NotificationType {
    WIN,
    LOSE,
    BROADCAST,
    INFO
}